import { HasPermissionsDirective } from './has-permissions.directive';

describe('HasPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
